/* 
  Add these @layer components to globals.css 
  (already included — this is a reference copy)
*/

/* 
  In globals.css, add:
  
  @layer components {
    .field-input {
      @apply w-full px-3 py-2 text-sm rounded-lg border border-brand-accent 
             focus:outline-none focus:border-brand-primary bg-white;
    }
    .field-label {
      @apply block text-xs font-medium text-gray-600 mb-1;
    }
  }
*/
